<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Omni.Client.Controls.NetworkErrorBanner"
             IsVisible="False">

    <Border BackgroundColor="#2D1A0A"
            StrokeThickness="0"
            Margin="20,0,20,12"
            x:Name="BannerRoot">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="14" />
        </Border.StrokeShape>

        <Grid ColumnDefinitions="Auto,*,Auto"
              Padding="14,10">

            <!-- Warning icon -->
            <Label Grid.Column="0"
                   Text="⚠"
                   FontSize="15"
                   TextColor="#F59E0B"
                   VerticalOptions="Center"
                   Margin="0,0,10,0" />

            <!-- Message -->
            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                <Label x:Name="TitleLabel"
                       Text="Can't reach server"
                       FontFamily="InterSemiBold"
                       FontSize="13"
                       TextColor="#F59E0B" />
                <Label x:Name="SubtitleLabel"
                       Text="Check your connection or backend."
                       FontSize="11"
                       TextColor="#A07030" />
            </VerticalStackLayout>

            <!-- Retry button -->
            <Button Grid.Column="2"
                    x:Name="RetryButton"
                    Text="Retry"
                    FontSize="12"
                    FontFamily="InterSemiBold"
                    TextColor="#F59E0B"
                    BackgroundColor="#3D2510"
                    Padding="12,6"
                    CornerRadius="8"
                    Clicked="OnRetryClicked" />
        </Grid>
    </Border>
</ContentView>
